name: lsp
version: 0.1.1
description: Tools for writing a server following the Language Server Protocol.
homepage: https://github.com/natebosch/dart_language_server

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  async: ^2.0.0
  json_rpc_2: ^2.0.3
  stream_channel: ^2.0.0

dev_dependencies:
  build_runner: ^1.0.0
  message_builder: ^0.1.6
  pedantic: ^1.9.0
  test: ^1.0.0
